# ============================================================================
# Production Environment Configuration
# ============================================================================
#
# PURPOSE:
#   Environment variables for the GCP production environment.
#   These will be injected into Cloud Run services at deployment time.
#
# USAGE:
#   1. Copy this file to .env.production
#   2. Replace placeholder values with actual production values
#   3. DO NOT commit .env.production to git (it's in .gitignore)
#   4. Store sensitive values in Secret Manager instead
#
# SECURITY NOTES:
#   - This file contains NO secrets (API keys, passwords)
#   - All secrets are stored in Secret Manager
#   - This file is safe to share with team (no sensitive data)
#
# DIFFERENCE FROM STAGING:
#   - ENVIRONMENT=production (instead of staging)
#   - Different database instance
#   - Different Cloud Run service names
#   - Potentially different resource limits
#
# ============================================================================

# ----------------------------------------------------------------------------
# Application Configuration
# ----------------------------------------------------------------------------
ENVIRONMENT=production
DEBUG=False
LOG_LEVEL=WARNING

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------
API_V1_STR=/api/v1

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------
# NOTE: DB_PASSWORD is stored in Secret Manager, not here
DB_HOST=/cloudsql/YOUR_PROJECT_ID:us-central1:ai-resume-review-db-prod
DB_PORT=5432
DB_NAME=ai_resume_review_prod
DB_USER=postgres
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=40

# ----------------------------------------------------------------------------
# Security Configuration
# ----------------------------------------------------------------------------
# NOTE: SECRET_KEY is stored in Secret Manager, not here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ----------------------------------------------------------------------------
# AI Configuration
# ----------------------------------------------------------------------------
# NOTE: OPENAI_API_KEY is stored in Secret Manager, not here
OPENAI_MODEL_NAME=gpt-4
OPENAI_MAX_TOKENS=4000
OPENAI_TEMPERATURE=0.3
OPENAI_REQUEST_TIMEOUT_SECONDS=30
LANGGRAPH_WORKFLOW_TIMEOUT_SECONDS=300
LANGGRAPH_MAX_RETRIES=2

# ----------------------------------------------------------------------------
# Monitoring & Observability
# ----------------------------------------------------------------------------
ENABLE_AI_WORKFLOW_LOGGING=True
AI_METRICS_COLLECTION_ENABLED=True

# ----------------------------------------------------------------------------
# Frontend Configuration (for NEXT_PUBLIC_ variables)
# ----------------------------------------------------------------------------
# These will be set in the frontend Cloud Run service
# NEXT_PUBLIC_API_URL=https://ai-resume-review-backend-prod-xxx.run.app
# NODE_ENV=production
# NEXT_TELEMETRY_DISABLED=1

# ============================================================================
# SECRETS (stored in Secret Manager)
# ============================================================================
# These are accessed via Secret Manager API, not environment variables:
#   - openai-api-key-prod
#   - db-password-prod
#   - jwt-secret-key-prod
#
# Access pattern in code:
#   from google.cloud import secretmanager
#   secret_value = get_secret("openai-api-key-prod")
# ============================================================================
